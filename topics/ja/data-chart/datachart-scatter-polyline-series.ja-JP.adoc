////
|metadata|
{
    "controlName": ["{GeoMapName}"],
    "tags": []
}
|metadata|
////

= 散布ポリライン シリーズの使用



=== 目的

このトピックでは、link:{DataChartLink}.{DataChartName}_members.html[{DataChartName}]™ コントロールで link:{DataChartLink}.ScatterPolylineSeries_members.html[ScatterPolylineSeries] 要素を使用する方法を提供します。

=== このトピックの内容

このトピックは、以下のセクションで構成されます。

* <<_Ref320651776, 散布ポリライン シリーズ >>

** <<_Ref320193474,概要>>
** <<_Ref320651783,プレビュー>>
** <<_Ref320651786,データ要件>>
** <<_Ref320651790,例>>

* <<_Ref320185294,関連コンテンツ>>

[[_Ref320651776]]
== 散布ポリライン シリーズ

[[_Ref320193474]]

=== 概要

{DataChartName} コントロールで、link:{DataChartLink}.ScatterPolylineSeries_members.html[ScatterPolylineSeries] はポリラインを使用してデータを表示するシリーズです。この散布シリーズのタイプは、ネットワーク グラフなどの切断された折れ線の描画が必要な場合に使用されます。link:{DataChartLink}.ScatterPolylineSeries_members.html[ScatterPolylineSeries] は、データが多角形の代わりにポリラインで描画されることを除いて、link:{DataChartLink}.ScatterPolygonSeries_members.html[ScatterPolygonSeries] とほどんど同様に機能します。

[[_Ref320651783]]

=== プレビュー

以下は、様々なポイントの間に接続を描画する link:{DataChartLink}.ScatterPolylineSeries_members.html[ScatterPolylineSeries] を持つ {DataChartName} コントロールのプレビューです。

image::images/DataChart_Using_Scatter_Polyline_Series_1.png[]

[[_Ref320651786]]

=== データ要件

{DataChartName} コントロールのシリーズの他のタイプと同様、link:{DataChartLink}.ScatterPolylineSeries_members.html[ScatterPolylineSeries] には、データ バインディングのための link:{DataChartLink}.series~{ApiDataSource}.html[{ApiDataSource}] プロパティがあります。このプロパティは、link:http://msdn.microsoft.com/ja-jp/library/system.collections.ienumerable.aspx[IEnumerable] インターフェイスを実装するオブジェクトにバインドできます。さらに、このオブジェクトの各項目には、link:http://msdn.microsoft.com/ja-jp/library/system.collections.ienumerable.aspx[IEnumerable]<link:http://msdn.microsoft.com/ja-jp/library/system.collections.ienumerable.aspx[IEnumerable]<pick:[wpf="link:http://msdn.microsoft.com/ja-jp/library/system.windows.point.aspx[Point]"]pick:[win-forms"{GeoMapNamespace}.Point"]>> 構造を使用して図形のポイント位置 (X と Y 値) を保存するデータ列が 1 つ必要です。このデータ列は、link:{DataChartLink}.ShapeSeriesBase~ShapeMemberPath.html[ShapeMemberPath] プロパティにマップされます。link:{DataChartLink}.ScatterPolylineSeries_members.html[ScatterPolylineSeries] は、{DataChartName} コントロールでポリラインをプロットするために、このマップされたデータ列のポイントを使用します。

[[_Ref320651790]]

=== 例

以上のスクリーンショットでのデータの構造は以下です。

*C# の場合:*
[source,csharp]
----
public class Node
{
    // The lines that branch off this node to other nodes.
    public List<List<Point>> Points { get; set; }
}
----

*Visual Basic の場合:*
[source,vb]
----
Public Class Node
    ' The lines that branch off this node to other nodes.
    Private _points As List(Of List(Of Point))
    public Property List(Of List(Of Point)) Points
        Get
            Return _points
        End Get
        Set
            _points = value
        End Set
    End Property
End Class
----

以下のコードは link:{DataChartLink}.ScatterPolylineSeries_members.html[ScatterPolylineSeries] をデータにバインドします。

ifdef::wpf[]
*XAML の場合:*

[source,xaml]
----
<ig:{DataChartName} x:Name="dataChart">
    <ig:{DataChartName}.Axes>
        <ig:NumericXAxis x:Name="xAxis"/>
        <ig:NumericYAxis x:Name="yAxis"/>
    </ig:{DataChartName}.Axes>
    <ig:{DataChartName}.Series>
        <ig:ScatterPolylineSeries ItemsSource="{Binding Path=Nodes}"
                                  ShapeMemberPath="Points"
                                  XAxis="{Binding ElementName=xAxis}"
                                  YAxis="{Binding ElementName=yAxis}"/>
    </ig:{DataChartName}.Series>
</ig:{DataChartName}>
----
endif::wpf[]

*Visual Basic の場合:*

[source,vb]
----
' ScatterPolylineSeries requires numeric X and Y axis
Dim xAxis = New NumericXAxis()
Dim yAxis = New NumericYAxis()
Me.dataChart.Axes.Add(xAxis)
Me.dataChart.Axes.Add(yAxis)

' create and set data binding to the ScatterPolylineSeries
Dim polylineSeries = New ScatterPolylineSeries()
polylineSeries.{ApiDataSource} = Nodes
polylineSeries.ShapeMemberPath = "Points"
polylineSeries.XAxis = xAxis
polylineSeries.YAxis = yAxis

' add the ScatterPolylineSeries to the the {DataChartName}
Me.dataChart.Series.Add(polylineSeries)
----

*C# の場合:*

[source,csharp]
----
// ScatterPolylineSeries requires numeric X and Y axis
var xAxis = new NumericXAxis();
var yAxis = new NumericYAxis();
this.dataChart.Axes.Add(xAxis);
this.dataChart.Axes.Add(yAxis);

// create and set data binding to the ScatterPolylineSeries
var polylineSeries = new ScatterPolylineSeries();
polylineSeries.{ApiDataSource} = Nodes;
polylineSeries.ShapeMemberPath = "Points";
polylineSeries.XAxis = xAxis;
polylineSeries.YAxis = yAxis;

// add the ScatterPolylineSeries to the the {DataChartName}
this.dataChart.Series.Add(polyLineSeries);
----

[[_Ref320185294]]
== 関連コンテンツ



このトピックに関連する追加情報については、以下のトピックを参照してください。

[options="header", cols="a,a"]
|====
|トピック|目的

ifdef::xaml[]
| link:datachart-creating-chart-in-xaml-code.html[XAML コードでチャートを作成] 
|このトピックでは、コントロールをアプリケーション ページに追加する方法を示します。
endif::xaml[]

| link:datachart-series-requirements.html[シリーズ要件]
|このトピックでは、{DataChartName} コントロールで利用可能な散布シリーズに関する情報を提供します。

|====