////
|metadata|
{
    "name": "categorychart-chart-types",
    "controlName": ["{CategoryChartName}"],
    "tags": [],
    "buildFlags": []
}
|metadata|
////

= チャート タイプ

== 概要

データの表示方法については、チャートの link:{CategoryChartLink}.{CategoryChartName}{ApiProp}charttype.html[ChartType] プロパティを設定します。
以下は、カテゴリ チャートでサポートされるすべてのタイプです。

特別なケースにプロパティの `Auto` 設定があります。`Auto` を使用した場合、チャートがデータを分析し、最も適したチャート タイプを割り当てます。

このプロパティのデフォルト値は、カテゴリ チャートにバインドされる基本の ItemsSource のサイズに基づいて決定されます。小さいデータセットが提供された場合、デフォルト値は `Column` です。より大きいデータ セットが提供された場合、デフォルト値は `Line` です。


== サポートされるチャート タイプ

[options="header", cols="a,a,a"]
|====
|プロパティ値|説明|例

|`Line`
|各データポイントでマーカーのカテゴリ折れ線シリーズを指定します。
|image::images/category_chart_line.png[height=300, width=500]
|`Area`
|カテゴリ エリア シリーズを指定します。
|image::images/category_chart_area.png[height=300, width=500]
|`Column`
|各データポイントで垂直の長方形のカテゴリ折れ線シリーズを指定します。
|image::images/category_chart_column.png[height=300, width=500]
|`Point`
|各データポイントでマーカーのカテゴリ ポイント チャートを指定します。
|image::images/category_chart_point.png[height=300, width=500]
|`StepLine`
|カテゴリ ステップ折れ線チャートを指定します。
|image::images/category_chart_stepline.png[height=300, width=500]
|`StepArea`
|カテゴリ ステップ エリア チャートを指定します。
|image::images/category_chart_steparea.png[height=300, width=500]
|`Spline`
|各データポイントでマーカーのカテゴリ スプライン シリーズを指定します。
|image::images/category_chart_spline.png[height=300, width=500]
|`SplineArea`
|カテゴリ スプライン エリア シリーズを指定します。
|image::images/category_chart_splinearea.png[height=300, width=500]
|`Waterfall`
|カテゴリ ウォーターフォール チャートを指定します。
|image::images/category_chart_waterfall.png[height=300, width=500]
|`Auto`
|Auto を使用した場合、チャートがデータを分析し、最も適したチャート タイプを割り当てます。
|

|====


== 関連トピック
* link:categorychart-walkthrough.html[カテゴリ チャートの追加]
* link:categorychart-configuring-chart-types.html[チャート タイプの構成]