= Chart Data Tooltip

In {ProductName}, the link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] displays values and titles of series as well as legend badges of series in a tooltip. In addition, it provides many configuration properties of the link:datachart-data-legend.html[DataLegend] for filtering series rows and values columns, styling, and formatting values. This tooltip type updates while moving the mouse inside of the plot area of the link:{CategoryChartLink}.{CategoryChartName}.html[{CategoryChartName}], link:{FinancialChartLink}.{FinancialChartName}.html[{FinancialChartName}], and link:{DataChartLink}.{DataChartName}.html[{DataChartName}] components.

== Data Tooltip Properties

All properties of link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] are prefixed with *DataToolTip* and exposed on API of link:{CategoryChartLink}.{CategoryChartName}.html[{CategoryChartName}] and link:{FinancialChartLink}.{FinancialChartName}.html[{FinancialChartName}] components. However, you will need to create an instance of link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] and add it to series collection of link:{DataChartLink}.{DataChartName}.html[{DataChartName}] component if you want to use it with Radial Charts, Polar Charts, Scatter Charts.

== Data Tooltip Elements

The link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] displays content using a set of three types of rows and four types of columns. 

=== Data Tooltip Rows

The rows of the link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] include the header row, series row(s), and the summary row.

The header row displays the axis label of the point that is hovered, and can be changed using the link:{DataChartLink}.DataToolTipLayer{ApiProp}HeaderText.html[HeaderText] property.

The series row can actually be a set of rows corresponding to each series plotted in the chart. These rows will display the legend badge, series title, actual/abbreviated value of the the series, and abbreviation symbol and unit, if specified.

Finally, there is a summary row that displays the total of all series values. The default summary title can be changed using the link:{DataChartLink}.DataToolTipLayer{ApiProp}SummaryTitleText.html[SummaryTitleText] property of the series. Also, you can use the link:{DataChartLink}.DataToolTipLayer{ApiProp}SummaryType.html[SummaryType] property to customize whether you display the Total, Min, Max, or Average of series values in the summary row.

The following code snippet demonstrates setting the properties mentioned above to have a link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] with a `Total` summary type with a custom title for the summary and a custom header:

ifdef::wpf[]

*In XAML*:

[source, xaml]
----
<ig:DataToolTipLayer HeaderText="My Custom Data Tooltip Header"
                     SummaryType="Total"
                     SummaryTitleText="Grand Total:"/>
----

endif::wpf[]

ifdef::wpf,win-forms[]

*In C#*:

[source, cs]
----
DataToolTipLayer tooltipLayer = new DataToolTipLayer()
{
    HeaderText = "My Custom Data Tooltip Header",
    SummaryType = DataLegendSummaryType.Total,
    SummaryTitleText = "Grand Total:"
};

dataChart1.Series.Add(tooltipLayer);            
----

endif::wpf,win-forms[]

=== Data Tooltip Columns

The columns of the data tooltip include the title, label, value, and units columns. Each series in the chart can have multiple columns for label, value, and units depending on the link:{DataChartLink}.DataToolTipLayer{ApiProp}IncludedColumns.html[IncludedColumns] or link:{DataChartLink}.DataToolTipLayer{ApiProp}ExcludedColumns.html[ExcludedColumns] collections of the tooltip layer.

The title column displays legend badges and series titles, which come from the `Title` property of the different `Series` plotted in the chart.

The label column displays the name or abbreviation of the different property paths in the link:{DataChartLink}.DataTooltipLayer~IncludedColumns.html[IncludedColumns] or link:{DataChartLink}.DataTooltipLayer~ExcludedColumns.html[ExcludedColumns] collections of the tooltip layer.

The value column displays series values as abbreviated text which can be formatted using the link:{DataChartLink}.DataToolTipLayer{ApiProp}ValueFormatAbbreviation.html[ValueFormatAbbreviation] property to apply the same abbreviation for all numbers by setting this property to `Auto` or `Shared`. Alternatively, a user can select other abbreviations such as `Independent`, `Kilo`, `Million`, etc. Precision of abbreviated values is controlled using the link:{DataChartLink}.DataToolTipLayer{ApiProp}ValueFormatMinFractions.html[ValueFormatMinFractions] and link:{DataChartLink}.DataToolTipLayer{ApiProp}ValueFormatMaxFractions.html[ValueFormatMaxFractions] for minimum and maximum digits, respectively.

The units column displays an abbreviation symbol and/or unit text, which can be set either on the link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] by setting the link:{DataChartLink}.DataToolTipLayer{ApiProp}UnitsText.html[UnitsText] for all columns or using the following properties on each series in the chart:

* Category Series (e.g. ColumnSeries)
    ** ValueMemberAsLegendUnit="K"

* Financial Price Series:
    ** OpenMemberAsLegendUnit="K"
    ** LowMemberAsLegendUnit="K"
    ** HighMemberAsLegendUnit="K"
    ** CloseMemberAsLegendUnit="K"
    
* Range Series:
    ** LowMemberAsLegendUnit="K"
    ** HighMemberAsLegendUnit="K"
        
* Radial Series:
    ** ValueMemberAsLegendUnit="km"

* Polar Series:
    ** RadiusMemberAsLegendUnit="km"
    ** AngleMemberAsLegendUnit="degrees"
    
For each of the above-listed properties, there is a corresponding `MemberAsLegendLabel` property as well to determine the text in the label columns mentioned previously.

The columns included in the link:{DataChartLink}.DataToolTipLayer{ApiProp}IncludedColumns.html[IncludedColumns] or link:{DataChartLink}.DataToolTipLayer{ApiProp}ExcludedColumns.html[ExcludedColumns] generally correspond to the value paths of your underlying data items, but the financial series has the option to include some special ones in addition to the `High`, `Low`, `Open`, and `Close` paths that are required for the financial series to plot correctly. You have the ability to show `TypicalPrice`, `Change`, and `Volume` options within the legend.
    
The following code snippet demonstrates a pair of `ColumnSeries` with the link:{DataChartLink}.AnchoredCategorySeries~ValueMemberAsLegendUnit.html[ValueMemberAsLegendUnit] properties set and the minimum/maximum fractions set on the link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer]:
    
ifdef::wpf[]

*In XAML*:
[source, xaml]
----
<ig:{DataChartName}>
    <ig:{DataChartName}.Axes>
        <ig:CategoryXAxis x:Name="xAxis"
                          ItemsSource="{Binding EnergySourceData}"
                          Label="{}{Location}" />
        <ig:NumericYAxis x:Name="yAxis" />
    </ig:{DataChartName}.Axes>
    <ig:{DataChartName}.Series>
        <ig:ColumnSeries XAxis="{Binding ElementName=xAxis}"
                         YAxis="{Binding ElementName=yAxis}"
                         ItemsSource="{Binding EnergySourceData}"
                         ValueMemberPath="Coal"
                         Title="Coal"
                         ValueMemberAsLegendUnit="K" />
        <ig:ColumnSeries XAxis="{Binding ElementName=xAxis}"
                         YAxis="{Binding ElementName=yAxis}"
                         ItemsSource="{Binding EnergySourceData}"
                         ValueMemberPath="Solar" 
                         Title="Solar"
                         ValueMemberAsLegendUnit="K"/>

        <ig:DataToolTipLayer ValueFormatMinFractions="2" 
                             ValueFormatMaxFractions="4"/>

    </ig:{DataChartName}.Series>
</ig:{DataChartName}>
----

endif::wpf[]

ifdef::wpf,win-forms[]

*In C#*:

[source, cs]
----
EnergyData energyDataSource = new EnergyData();

CategoryXAxis xAxis = new CategoryXAxis()
{
    ItemsSource = energyDataSource,
    Label = "Location"
};

NumericYAxis yAxis = new NumericYAxis();

ColumnSeries series1 = new ColumnSeries()
{
    XAxis = xAxis,
    YAxis = yAxis,
    ItemsSource = energyDataSource,
    ValueMemberPath = "Coal",
    ValueMemberAsLegendUnit = "K"
};

ColumnSeries series2 = new ColumnSeries()
{
    XAxis = xAxis,
    YAxis = yAxis,
    ItemsSource = energyDataSource,
    ValueMemberPath = "Solar",
    ValueMemberAsLegendUnit = "K"
};

DataToolTipLayer tooltipLayer = new DataToolTipLayer()
{
    ValueFormatMinFractions = 2,
    ValueFormatMaxFractions = 4
};

dataChart1.Axes.Add(xAxis);
dataChart1.Axes.Add(yAxis);

dataChart1.Series.Add(series1);
dataChart1.Series.Add(series2);
dataChart1.Series.Add(tooltipLayer);  
----

endif::wpf,win-forms[]

== Layout Mode

Legend items can be positioned in a vertical or table structure via the link:{DataChartLink}.DataToolTipLayer{ApiProp}LayoutMode.html[LayoutMode] property. The default value is `Table`, which retains the same look and feel as seen in previous releases.

eg.

image::images/Layout_Mode.png[]

== Data Tooltip Styling

The link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] provides properties for styling each type of column. Each of these properties begins with Title, Label, Value, or Units, and you can style the text's color, font, and margin. For example, if you wanted to set the text color of each of these, you would set the link:{DataChartLink}.DataToolTipLayer{ApiProp}TitleTextColor.html[TitleTextColor], link:{DataChartLink}.DataToolTipLayer{ApiProp}LabelTextColor.html[LabelTextColor], link:{DataChartLink}.DataToolTipLayer{ApiProp}ValueTextColor.html[ValueTextColor], and link:{DataChartLink}.DataToolTipLayer{ApiProp}UnitsTextColor.html[UnitsTextColor] properties.

The following code snippet demonstrates how to set the styling properties mentioned above:

ifdef::wpf[]

*In XAML*:
[source,xaml]
----
<ig:DataToolTipLayer TitleTextColor="LightGray"
                     LabelTextColor="LightGray"
                     ValueTextColor="Green"
                     UnitsTextColor="Green"
                     UnitsText="K"/>
----

endif::wpf[]

ifdef::wpf,win-forms[]

*In C#*:

[source, cs]
----
DataToolTipLayer tooltipLayer = new DataToolTipLayer()
{
    TitleTextColor = Brushes.LightGray,
    LabelTextColor = Brushes.LightGray,
    ValueTextColor = Brushes.Green,
    UnitsTextColor = Brushes.Green,
    UnitsText = "K"
};

dataChart1.Series.Add(tooltipLayer);
----

endif::wpf,win-forms[]

== Data Tooltip Grouping & Positioning

You can set the link:{DataChartLink}.DataToolTipLayer{ApiProp}GroupingMode.html[GroupingMode] property to either `Grouped` or `Individual` to group content for multiple series into single tooltip or separate content for each series in multiple tooltips. In the `Grouped` mode, you can customize where the tooltip is shown by setting the link:{DataChartLink}.DataToolTipLayer{ApiProp}GroupedPositionModeX.html[GroupedPositionModeX] and link:{DataChartLink}.DataToolTipLayer{ApiProp}GroupedPositionModeY.html[GroupedPositionModeY] properties. This essentially allows you to customize the horizontal and vertical alignments of the tooltip and whether you want it to track to the closest series points to the mouse position or pin the tooltip to edge of plot area.

The following code snippet demonstrates a link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] that will be pinned to the top-left of the chart as you scroll:

ifdef::wpf[]

*In XAML*:
[source,xaml]
----
<ig:DataToolTipLayer GroupingMode="Grouped"
                     GroupedPositionModeX="PinLeft"
                     GroupedPositionModeY="PinTop"/>
----

endif::wpf[]

ifdef::wpf,win-forms[]

*In C#*:

[source, cs]
----
DataToolTipLayer tooltipLayer = new DataToolTipLayer()
{
    GroupingMode = DataToolTipLayerGroupingMode.Grouped,
    GroupedPositionModeX = DataTooltipGroupedPositionX.PinLeft,
    GroupedPositionModeY = DataTooltipGroupedPositionY.PinTop
};

dataChart1.Series.Add(tooltipLayer);
----

endif::wpf,win-forms[]

== Data Tooltip Value Formatting

The link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] provides automatic abbreviation of large numbers using its link:{DataChartLink}.DataToolTipLayer{ApiProp}ValueFormatAbbreviation.html[ValueFormatAbbreviation] property. This adds a multiplier in the units column such as kilo, million, billion, etc. You can customize the number of fractional digits that are displayed by setting the link:{DataChartLink}.DataToolTipLayer{ApiProp}ValueFormatMinFractions.html[ValueFormatMinFractions] and link:{DataChartLink}.DataToolTipLayer{ApiProp}ValueFormatMaxFractions.html[ValueFormatMaxFractions]. This will allow you to determine the minimum and maximum number of digits that appear after the decimal point, respectively.

The following code snippet demonstrates how to set the minimum and maximum fractions of the link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer]:

ifdef::wpf[]

*In XAML*:
[source,xaml]
----
<ig:DataToolTipLayer ValueFormatMinFractions="2" 
                     ValueFormatMaxFractions="4"/>
----

endif::wpf[]

ifdef::wpf,win-forms[]

*In C#*:

[source, cs]
----
DataToolTipLayer tooltipLayer = new DataToolTipLayer()
{
    ValueFormatMinFractions = 2,
    ValueFormatMaxFractions = 4
};

dataChart1.Series.Add(tooltipLayer);
----

endif::wpf,win-forms[]

== Data Tooltip Value Mode

You can change the default decimal display of values within the link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] to be currency by changing the link:{DataChartLink}.DataToolTipLayer{ApiProp}ValueFormatMode.html[ValueFormatMode] property of the layer. The link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] also exposes the ability to modify the culture of the displayed currency symbol by using its link:{DataChartLink}.DataToolTipLayer{ApiProp}ValueFormatCulture.html[ValueFormatCulture] property and setting it to its corresponding culture tag.

For example, the following code snippet will create a link:{DataChartLink}.DataToolTipLayer.html[DataTooltipLayer] with the link:{DataChartLink}.DataToolTipLayer{ApiProp}ValueFormatCulture.html[ValueFormatCulture] set to "en-GB" and the link:{DataChartLink}.DataToolTipLayer{ApiProp}ValueFormatMode.html[ValueFormatMode] set to "Currency":

ifdef::wpf[]

*In XAML*:
[source,xaml]
----
<ig:DataToolTipLayer ValueFormatCulture="en-GB"
                     ValueFormatMode="Currency"/>
----

endif::wpf[]

ifdef::wpf,win-forms[]

*In C#*:

[source, cs]
----
DataToolTipLayer tooltipLayer = new DataToolTipLayer()
{
    ValueFormatCulture = "en-GB",
    ValueFormatMode = DataLegendValueMode.Currency
};

dataChart1.Series.Add(tooltipLayer);
----

endif::wpf,win-forms[]

== Data Tooltip Grouping

link:{DataChartLink}.Series~DataLegendGroup.html[DataLegendGroup] can be set, on all types of series, to a string that will categorize a group of series in Data Legend. Each group will have its own summary row displayed before another group of series is displayed: 
By default, DataLegend will hide names of groups, but you can display group names by setting the - link:{DataChartLink}.DataToolTipLayer{ApiProp}GroupRowVisible.html[GroupRowVisible] property to true. link:{DataChartLink}.DataToolTipLayer{ApiProp}GroupingMode.html[GroupingMode] and link:{DataChartLink}.DataToolTipLayer{ApiProp}LabelDisplayMode.html[LabelDisplayMode] should be set to "Visible" on the Data Tooltip Layer.

Several properties are exposed including grouping portions of the legend. 

- link:{DataChartLink}.{DataLegendName}{ApiProp}GroupRowMargin.html[GroupRowMargin]
- link:{DataChartLink}.{DataLegendName}{ApiProp}GroupTextMargin.html[GroupTextMargin]
- link:{DataChartLink}.{DataLegendName}{ApiProp}GroupTextColor.html[GroupTextColor]
- link:{DataChartLink}.{DataLegendName}{ApiProp}GroupTextFontSize.html[GroupTextFontSize]
- link:{DataChartLink}.{DataLegendName}{ApiProp}GroupTextFontStyle.html[GroupTextFontStyle]
- link:{DataChartLink}.{DataLegendName}{ApiProp}GroupTextFontStretch.html[GroupTextFontStretch]
- link:{DataChartLink}.{DataLegendName}{ApiProp}GroupTextFontWeight.html[GroupTextFontWeight]
- link:{DataChartLink}.{DataLegendName}{ApiProp}HeaderTextMargin.html[HeaderTextMargin]
- link:{DataChartLink}.{DataLegendName}{ApiProp}HeaderTextColor.html[HeaderTextColor]
- link:{DataChartLink}.{DataLegendName}{ApiProp}HeaderTextFontSize.html[HeaderTextFontSize]
- link:{DataChartLink}.{DataLegendName}{ApiProp}HeaderTextFontFamily.html[HeaderTextFontFamily]
- link:{DataChartLink}.{DataLegendName}{ApiProp}HeaderTextFontStyle.html[HeaderTextFontStyle]
- link:{DataChartLink}.{DataLegendName}{ApiProp}HeaderTextFontStretch.html[HeaderTextFontStretch]
- link:{DataChartLink}.{DataLegendName}{ApiProp}HeaderTextFontWeight.html[HeaderTextFontWeight]